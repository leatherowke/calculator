console.log("welcome to the password validator!");
import { createInterface } from "readline";


const r1 = createInterface({
    input: process.stdin,
    output: process.stdout
};)

let userInput = "";

r1.question("enter password",function (userInput){
    userInput = string;

    console.log("password" + userInput);
    

r1.close();
});